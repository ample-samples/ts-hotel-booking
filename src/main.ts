const world = 'world';
export function hello(who: string = world): string {
  return `Hello ${who}! `;
}
console.log(hello())


const roomObj = { roomSize: capacity, occupancy: fullness}

const roomVacanies = []
